# Workflow

1. Run install + verify
2. User requests weather/price snapshot
3. Agent runs `scripts/recommend.sh`
4. User requests model switch
5. Agent runs `scripts/switch-model.sh` (whitelist validated)
